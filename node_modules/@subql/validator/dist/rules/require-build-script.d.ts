import { Context } from '../context';
import { Rule, RuleType } from './rule';
export declare class RequireBuildScript implements Rule {
    type: RuleType;
    name: string;
    description: string;
    validate(ctx: Context): boolean;
}
