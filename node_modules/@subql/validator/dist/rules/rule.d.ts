import { Context } from '../context';
export declare enum RuleType {
    PackageJSON = "packageJSON",
    Schema = "schema"
}
export interface Rule {
    type: RuleType;
    name: string;
    description: string;
    validate(ctx: Context): boolean | Promise<boolean>;
}
export declare const commonRules: Rule[];
