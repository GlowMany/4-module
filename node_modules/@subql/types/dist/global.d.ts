import { ApiPromise } from '@polkadot/api';
import { ApiDecoration } from '@polkadot/api/types';
import Pino from 'pino';
import { Store } from './interfaces';
declare type ApiAt = ApiDecoration<'promise'> & {
    rpc: ApiPromise['rpc'];
};
declare global {
    const api: ApiAt;
    const logger: Pino.Logger;
    const store: Store;
}
export {};
