import { performance, performanceCmp } from '@polkadot/util/test/performance';
declare type ExecFn = (input: Uint8Array, onlyJs: boolean) => unknown;
export declare function performanceWasm(name: string, count: number, exec: ExecFn, inputs?: Uint8Array[][]): void;
export { performance, performanceCmp };
